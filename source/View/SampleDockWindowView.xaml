<UserControl x:Class="iMS_Studio_first_attempt.View.SampleDockWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:iMS_Studio_first_attempt.View"
             xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <xcdg:DataGridCollectionViewSource x:Key="image_data"
                                  Source="{Binding Source={x:Static Application.Current},
                                                      Path=ImageData}"/>
        </Grid.Resources>
        <xcdg:DataGridControl x:Name="ImagePoints" ItemsSource="{Binding Source={StaticResource image_data}}"/>
    </Grid>
</UserControl>
