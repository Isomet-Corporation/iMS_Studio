<UserControl x:Class="iMS_Studio.View.SequenceDockWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:iMS_Studio.View"
             xmlns:vm="clr-namespace:iMS_Studio.ViewModel"
             xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
             xmlns:xcdg="clr-namespace:Xceed.Wpf.DataGrid;assembly=DotNetProjects.Wpf.DataGrid"
             xmlns:xcdgv="clr-namespace:Xceed.Wpf.DataGrid.Views;assembly=DotNetProjects.Wpf.DataGrid"
             xmlns:xcdgm="clr-namespace:Xceed.Wpf.DataGrid.Markup;assembly=DotNetProjects.Wpf.DataGrid"
             xmlns:xctk="clr-namespace:Xceed.Wpf.Toolkit;assembly=DotNetProjects.Wpf.Extended.Toolkit"
             xmlns:ims="clr-namespace:iMS;assembly=iMSNET"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <xcdg:DataGridCollectionViewSource x:Key="seq_data" 
                                  Source="{Binding Path=SequenceData}" />

            <xcdg:DataGridCollectionViewSource x:Key="name_data" 
                                  Source="{Binding Path=ImageNames}"/>

            <DataTemplate x:Key="Image_celltemplate">
                <TextBlock Text="{Binding}" HorizontalAlignment="Center"/>
            </DataTemplate>
            <DataTemplate x:Key="Image_edittemplate">
                <ComboBox x:Name="ImageComboBox" HorizontalAlignment="Center"
                          ItemsSource="{Binding Source={StaticResource name_data}}" 
                          DisplayMemberPath="FileName" SelectedValuePath="FileName" 
                          SelectedValue="{Binding ImageName, Source={StaticResource seq_data}, Mode=TwoWay}">
                </ComboBox>
            </DataTemplate>
            <DataTemplate x:Key="double_celltemplate">
                <TextBlock Text="{Binding Path=., Mode=TwoWay, StringFormat={}{0:f4}}" HorizontalAlignment="Center"/>
            </DataTemplate>
            <DataTemplate x:Key="default_celltemplate">
                <TextBlock Text="{Binding}" HorizontalAlignment="Center"/>
            </DataTemplate>
            <DataTemplate x:Key="numrpts_edittemplate">
                <xctk:IntegerUpDown Value="{xcdgm:CellEditorBinding}" Minimum="0" Maximum="255" />
            </DataTemplate>
            <DataTemplate x:Key="extdiv_edittemplate">
                <xctk:IntegerUpDown Value="{xcdgm:CellEditorBinding}" Minimum="0" Maximum="65535" />
            </DataTemplate>

            <ObjectDataProvider MethodName="GetValues"
                ObjectType="{x:Type sys:Enum}"
                x:Key="ImageRepeatEnumValues">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="ims:ImageRepeats" />
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>
            <DataTemplate x:Key="Repeat_edittemplate">
                <ComboBox x:Name="RepeatComboBox" HorizontalAlignment="Center"
                          ItemsSource="{Binding Source={StaticResource ImageRepeatEnumValues}}" 
                          SelectedItem="{Binding RptType, Source={StaticResource seq_data}, Mode=TwoWay}">
                </ComboBox>
            </DataTemplate>

            <ObjectDataProvider MethodName="GetValues"
                ObjectType="{x:Type sys:Enum}"
                x:Key="TermActionEnumValues">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="ims:SequenceTermAction" />
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>

            <Style TargetType="{x:Type xcdg:ColumnManagerCell}">
                <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ColumnHeader_PreviewMouseButtonDown"/>
            </Style>
        </Grid.Resources>

        <xcdg:DataGridControl x:Name="SequenceEntries" Grid.Row="0" ItemsSource="{Binding Source={StaticResource seq_data}}"
                              AutoCreateColumns="False"
                              PreviewKeyDown="SequenceEntries_KeyDown" PreviewKeyUp="SequenceEntries_KeyUp">
            <xcdg:DataGridControl.Columns>
                <xcdg:Column FieldName="ImageName" Title="Image" CellContentTemplate="{StaticResource Image_celltemplate}">
                    <xcdg:Column.CellEditor>
                        <xcdg:CellEditor EditTemplate="{StaticResource Image_edittemplate}" />
                    </xcdg:Column.CellEditor>
                </xcdg:Column>
                <xcdg:Column FieldName="IntOsc" Title="Internal Clock Rate (kHz)" CellContentTemplate="{StaticResource double_celltemplate}"/>
                <xcdg:Column FieldName="ExtDiv" Title="External Clock Divide" CellContentTemplate="{StaticResource default_celltemplate}">
                    <xcdg:Column.CellEditor>
                        <xcdg:CellEditor EditTemplate="{StaticResource extdiv_edittemplate}"/>
                    </xcdg:Column.CellEditor>
                </xcdg:Column>
                <xcdg:Column FieldName="RptType" Title="Repeat" CellContentTemplate="{StaticResource default_celltemplate}">
                    <xcdg:Column.CellEditor>
                        <xcdg:CellEditor EditTemplate="{StaticResource Repeat_edittemplate}"/>
                    </xcdg:Column.CellEditor>
                </xcdg:Column>
                <xcdg:Column FieldName="NumRpts" Title="Program Repeats" CellContentTemplate="{StaticResource default_celltemplate}">
                    <xcdg:Column.CellEditor>
                        <xcdg:CellEditor EditTemplate="{StaticResource numrpts_edittemplate}"/>
                    </xcdg:Column.CellEditor>
                </xcdg:Column>
                <xcdg:Column FieldName="PostImgDelay" Title="Post-Image Delay(ms)" CellContentTemplate="{StaticResource double_celltemplate}"/>
                <xcdg:Column FieldName="SyncOutDelay" Title="Sync Output Delay(us)" CellContentTemplate="{StaticResource double_celltemplate}"/>
                </xcdg:DataGridControl.Columns>

            <xcdg:DataGridControl.CommandBindings>
                <CommandBinding Command="{x:Static ApplicationCommands.Paste}"/>
            </xcdg:DataGridControl.CommandBindings>
            <xcdg:DataGridControl.ItemScrollingBehavior>Immediate</xcdg:DataGridControl.ItemScrollingBehavior>

            <xcdg:DataGridControl.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{x:Static ApplicationCommands.Copy}" >
                        <MenuItem.Icon>
                            <Image Source="..\..\resources\Images\copy.png" Height="16" Width="16"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Command="{x:Static ApplicationCommands.Paste}" >
                        <MenuItem.Icon>
                            <Image Source="..\..\resources\Images\paste.png" Height="16" Width="16"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="_Insert Rows" Command="{Binding Path=InsertNewImageRowsCommand}"
                              CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}},
                              Path=PlacementTarget.SelectedCellRanges}" >
                    </MenuItem>
                    <MenuItem Header="_Delete Rows" Command="{Binding Path=DeleteImageRowsCommand}"
                              CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}},
                              Path=PlacementTarget.SelectedCellRanges}" >
                    </MenuItem>
                </ContextMenu>
            </xcdg:DataGridControl.ContextMenu>

            <xcdg:DataGridControl.View>
                <xcdgv:TableView FixedColumnCount="1" UseDefaultHeadersFooters="False" ShowRowSelectorPane="True">
                    <xcdgv:TableView.FixedHeaders>
                        <DataTemplate>
                            <xcdg:ColumnManagerRow AllowSort="False" AllowColumnReorder="True"/>
                        </DataTemplate>
                    </xcdgv:TableView.FixedHeaders>
                </xcdgv:TableView>
            </xcdg:DataGridControl.View>
        </xcdg:DataGridControl>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,8,10,0">
            <Label Content="Number of Sequence Entries: " />
            <xctk:IntegerUpDown x:Name="seqCount" Value="{Binding SeqCount, Mode=TwoWay}" Minimum="1" />
            <Label Content="Action at End of Sequence: " />
            <ComboBox x:Name="TermActionComboBox" Margin="10,0,50,0"
                          ItemsSource="{Binding Source={StaticResource TermActionEnumValues}}" 
                          SelectedItem="{Binding TermAction, Mode=TwoWay}" SelectionChanged="TermActionComboBox_SelectionChanged">
            </ComboBox>
            <Label Content="Value: " />
            <xctk:IntegerUpDown x:Name="TermValueSelector" Value="{Binding TermValue, Mode=TwoWay}" Minimum="1" Maximum="{Binding Path=Count, Source={StaticResource seq_data}}"/>
        </StackPanel>
    </Grid>
</UserControl>
